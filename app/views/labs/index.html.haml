- title "Labs"
= render "header"

.row
  .columns.large-3.sidebar.hide-for-small
    = render "sidebar"
  .columns.large-9.labs
    #no-results
      Sorry, there are no labs that matched that search term

    -# - if action_name == 'index'
    -#   .row
    -#     .columns.large-8.push-4.text-right
    -#       .sort
    -#         Sort by:
    -#         = sort_link @q, :name
    -#         = sort_link @q, :created_at, "Date Added"

    - @lab_countries.sort.each do |country, labs|
      %fieldset.country
        %legend
          .flag{class: "flag-#{country}"}
          = Country.new(country.upcase).name
        - labs.sort_by(&:name).each do |lab|
          = content_tag_for :div, lab, data: {name: lab.name.downcase, region: lab.region.downcase, kind: lab.kind } do
            -# .columns.large-2
            -#   - if lab.avatar.blank?
            -#     = holder_tag 80
            -#   - else
            -#     = image_tag lab.avatar, width: 80, height: 80

            .columns.large-7.details
              .name
                = link_to lab, lab
                -# = Time.now.in_time_zone(lab.time_zone).stamp("15:00")
                -# %span.kind
                -#   - if lab.kind.present?
                -#     = Lab::Kinds[lab.kind]

              .address.hint= fa_icon "map-marker", text: lab.short_address, class: 'icon-fixed-width'
              -# .times.hint= fa_icon "time", text: 'Open 10:00 - 14:00', class: 'icon-fixed-width'
            -# .columns.large-4.hide-for-small
            -#   - 3.times do |index|
            -#     - if lab.humans[index]
            -#       = image_tag lab.humans[index].user.avatar_image, width: 55, class: 'has-tip', data: {tooltip: true}, title: "#{lab.humans[index].user.full_name} - #{lab.humans[index].details}"
            -#     -# - else
            -#     -#   = holder_tag 55

            -# .columns.large-5.hide-for-small
            -#   - if lab.humans.any?
            -#     = image_tag lab.humans.first.user.avatar_image, class: 'has-tip image', data: {tooltip: true}, title: "#{lab.humans.first.user.full_name} - #{lab.humans.first.details}"
            -#     .contact
            -#       .name= link_to lab.humans.first.user.full_name, lab.humans.first.user
            -#       .phone.hint 0181819400913
            -#       .email= mail_to "john@bitsushi.com"
