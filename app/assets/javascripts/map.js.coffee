# jQuery ->
#   $('#map-filters input').change ->
#     # inBounds = []
#     # re = new RegExp(string,"i")
#     # for marker in @markers
#     #   if re.test "#{marker.options.address} #{marker.options.name}"
#     #     inBounds.push {
#     #       name: marker.options.name,
#     #       url: marker.options.url,
#     #       id: marker.options.id,
#     #       address: marker.options.address
#     #     }
#     # @scope.labs = inBounds
#     # @scope.$apply()